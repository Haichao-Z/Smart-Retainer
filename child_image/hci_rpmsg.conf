#
# cpunet (hci_rpmsg) 配置
# 修复 "n must be >= 0" 错误
#

# 1. 增加缓冲区数量
CONFIG_BT_BUF_ACL_TX_COUNT=8
CONFIG_BT_BUF_ACL_RX_COUNT=8

# 2. 增加缓冲区大小 (251 字节)
CONFIG_BT_BUF_ACL_TX_SIZE=251
CONFIG_BT_BUF_ACL_RX_SIZE=251

# 3. 设置协议栈的 GATT ATT MTU (关键!)
# 251 (缓冲区) - 4 (L2CAP 头) = 247
CONFIG_BT_ATT_MTU=247